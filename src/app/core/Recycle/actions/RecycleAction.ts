import { createAction } from "@ngrx/store";

export const LoadStateRecycleComponent = createAction(
    '[Recycle Layout Component / Load]'
)

export const Open = createAction(
    '[Recycle Layout Component / Open]'
)

export const Close = createAction(
    '[Recycle Layout Component / Close]'
)